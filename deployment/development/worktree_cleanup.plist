<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.mini-infra.worktree-cleanup</string>

    <!-- Use a login shell so pnpm/node resolve via the user's PATH. -->
    <key>ProgramArguments</key>
    <array>
        <string>/bin/zsh</string>
        <string>-lc</string>
        <string>cd 'REPO_ROOT' && exec pnpm --silent worktree-env cleanup</string>
    </array>

    <key>WorkingDirectory</key>
    <string>REPO_ROOT</string>

    <!-- Run once an hour -->
    <key>StartInterval</key>
    <integer>3600</integer>

    <key>StandardOutPath</key>
    <string>MINI_INFRA_HOME/worktree-cleanup.log</string>

    <key>StandardErrorPath</key>
    <string>MINI_INFRA_HOME/worktree-cleanup.log</string>

    <!-- Only run when the user is logged in -->
    <key>RunAtLoad</key>
    <false/>
</dict>
</plist>
