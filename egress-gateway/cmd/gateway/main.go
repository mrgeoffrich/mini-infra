// Package main is the entry point for the egress-gateway binary.
// Phase 3 will implement the Smokescreen-based forward proxy here.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "egress-gateway: phase 3 not yet implemented")
	os.Exit(0)
}
