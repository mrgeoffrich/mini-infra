{
  "name": "mini-infra",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "lib"
  ],
  "scripts": {
    "dev": "node scripts/dev-with-restart.mjs",
    "dev:all": "concurrently \"npm run dev:lib\" \"npm run dev:server\" \"npm run dev:client\"",
    "dev:lib": "npm run dev -w lib",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "dev:agent-sidecar": "node scripts/dev-agent-sidecar.mjs",
    "build": "npm run build:server && npm run build:client",
    "build:lib": "npm run build -w lib",
    "build:client": "npm run build -w client",
    "build:server": "npm run build:lib && npm run build -w server",
    "build:sidecar": "npm --prefix update-sidecar install && npm --prefix update-sidecar run build",
    "build:agent-sidecar": "npm --prefix agent-sidecar install && npm --prefix agent-sidecar run build",
    "build:pg-backup": "docker build -t mini-infra-pg-backup:dev ./pg-az-backup",
    "build:all": "npm run build:lib && concurrently \"npm run build:client\" \"npm run build:server\" \"npm run build:sidecar\" \"npm run build:agent-sidecar\"",
    "start": "npm start -w server",
    "install:all": "npm install",
    "generate:ui-manifest": "node scripts/generate-ui-manifest.mjs",
    "format": "npm run format -w server",
    "format:check": "npm run format:check -w server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "cross-env": "^10.0.0"
  }
}
