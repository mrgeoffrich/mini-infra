{
  "name": "@mini-infra/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Mini Infra client and server",
  "main": "types/index.js",
  "types": "types/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}