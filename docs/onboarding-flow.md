I want to think about authentication in this application. at the moment its very basic - I also want a nice onboarding flow that runs when the application is not configured for authentication yet, like when a user first runs the application and wants to set it up. Auth settings also need to persist in a way such that a user can change them outside the application, cause if it breaks they wont be able to log in. I want you to ideate and design a set of UIs and functinality to achieve this. I want to support Google Auth, Github Auth, Microsoft auth (Entra ID).
